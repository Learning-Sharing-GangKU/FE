// Re-exports for backward compatibility — use @/types/* directly
export type { GatheringItem, GatheringSummary, GatheringDetailResponse, GatheringParticipant } from '@/types/gathering';
export type { UserProfile, ReviewItem, ReviewsMeta } from '@/types/user';
